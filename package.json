{
  "name": "arrows-3d",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "build:analyze": "npm run build && start stats.html",
    "preview": "vite preview",
    "serve": "vite preview --port 3000 --open"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^6.0.5",
    "terser": "^5.44.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vite-plugin-compression": "^0.5.1"
  },
  "dependencies": {
    "@babylonjs/core": "^8.43.0",
    "@babylonjs/loaders": "^8.43.0"
  }
}
