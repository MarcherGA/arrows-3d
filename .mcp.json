{
  "mcpServers": {
    "replicate": {
      "command": "node",
      "args": ["./node_modules/replicate-mcp/index.js"],
      "env": {
        "REPLICATE_API_TOKEN": "${REPLICATE_API_TOKEN}"
      }
    }
  }
}
